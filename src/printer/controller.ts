export const sendToPrinter = (labels) => {
    console.log('Printer',labels)
    //conect printer to send files
    //wait response or error to send to alexa
    return Promise.resolve('LABELS PRINTED')
}